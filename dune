(executable
  (public_name kamp)
  (name main)
  (modules app song state file persist api ui main)
  (libraries unix directories raylib audio_file)
  (link_flags (:include link_flags.sexp))
)

(rule
  (targets link_flags.sexp)
  (action (run ./config/config.exe))
)
