(executable
  (public_name kamp)
  (name main)
  (modules song state persist api ui main)
  (libraries unix directories raylib)
  (link_flags (:include link_flags.sexp))
)

(rule
  (targets link_flags.sexp)
  (action (run ./config/config.exe))
)
